version: '3.1'
services:
  db:
    image: postgres
    restart: always
    container_name: postgres-docker # 컨테이너 이름 설정
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: test
#    volumes:
#      - ../data:/var/lib/postgresql/data
#      - ./init/:/docker-entrypoint-initdb.d/
    ports:
      - 5432:5432

#  adminer:
#    image: adminer
#    restart: always
#    ports:
#      - 8080:8080